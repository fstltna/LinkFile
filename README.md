# LinkFile - version 1.0.0
Creates a link of a given file to a given destination directory

Requires the following Perl files:
*	cpan -i Cwd qw(cwd)

You can install these by running:
	./installdeps

Usage: linkfile.pl \<FILE\> \<DOWNLOADDIR\>
